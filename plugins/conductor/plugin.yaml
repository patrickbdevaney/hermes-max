name: conductor
version: 1.0.0
description: "Deterministic conductor↔executor split via lifecycle hooks: re-injects the execution contract into every LLM call (survives context compaction), detects verify-pass/fail + file writes, and fires the cloud synth cascade on stuck-detection — all in-process, no subprocess. Emits a live event feed to the cockpit/UI."
author: "hermes-max"
hooks:
  - pre_llm_call
  - post_llm_call
  - post_tool_call
  - on_session_end
