mcp>=1.2.0
httpx>=0.27
# trafilatura is the LOCAL fallback extractor (used only if Crawl4AI is down).
# It is lazy-installed on first fallback via lib/lazy_install.py, but declaring
# it here means bootstrap installs it up front so the fallback never blocks.
trafilatura>=1.8
opentelemetry-sdk>=1.20
opentelemetry-exporter-otlp-proto-grpc>=1.20
