"""hermes-max UI — hybrid web/desktop interface layer (see CLAUDE_ui.md).

Tier 1 (this package's `server/` + `web/`) is the minimum-lovable localhost web
view, served by `hm ui`, rendering L0 ambient + L1 timeline + live cost off the
existing livelog and cost ledger.
"""
